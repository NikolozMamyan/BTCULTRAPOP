{% extends 'admin/base.html.twig' %}

{% block admin_title %}{{ 'admin.product.new.title'|trans }}{% endblock %}

{% block admin_content %}
    <section class="admin-product-editor">
        <div class="admin-page-heading admin-page-heading--actions">
            <div>
                <span>{{ 'admin.product.new.eyebrow'|trans }}</span>
                <h1>{{ 'admin.product.new.title'|trans }}</h1>
                <p>{{ 'admin.product.new.subtitle'|trans }}</p>
            </div>
            <a href="{{ path('app_admin_products_index') }}" class="admin-secondary-button">
                <i class="fa-solid fa-arrow-left"></i>
                {{ 'admin.product.back_to_list'|trans }}
            </a>
        </div>

        {% include 'admin/products/_form.html.twig' with {
            form: form,
            product: product,
            submit_label: 'admin.product.form.create_submit',
            mode: 'new'
        } only %}
    </section>
{% endblock %}
